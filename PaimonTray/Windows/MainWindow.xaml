<Window
    mc:Ignorable="d"
    x:Class="PaimonTray.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="using:H.NotifyIcon"
    xmlns:vm="using:PaimonTray.ViewModels"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <StackPanel.DataContext>
            <vm:TaskbarIconViewModel x:Name="TaskbarIconViewModel" />
        </StackPanel.DataContext>
        <tb:TaskbarIcon
            DoubleClickCommand="{x:Bind TaskbarIconViewModel.ShowMainWindowCommand}"
            LeftClickCommand="{x:Bind TaskbarIconViewModel.ShowMainWindowCommand}"
            IconSource="../Assets/AppIcon/AppIcon.ico"
            x:Name="TaskbarIconApp" />
    </StackPanel>
</Window>